[ "$NUM1" -eq "$NUM2" ] # True if n1 and n2 are equal.
[ "$NUM1" -ne "$NUM2" ] # True if n1 and n2 are not equal.
[ "$NUM1" -lt "$NUM2" ] # True if n1 is less than the n2.
[ "$NUM1" -le "$NUM2" ] # True if n1 is less than or equal to the n2.
[ "$NUM1" -gt "$NUM2" ] # True if n1 is greater than the n2.
[ "$NUM1" -ge "$NUM2" ] # True if n1 is greater than or equal to the n2.
